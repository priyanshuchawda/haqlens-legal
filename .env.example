# Safe default. Set to gemini only when a server-side key is configured.
AI_PROVIDER=disabled
PORT=3001

# Required only when AI_PROVIDER=gemini. Never commit a real value.
GEMINI_API_KEY=
# Optional model override. The default is gemini-3.5-flash-lite.
GEMINI_MODEL=gemini-3.5-flash-lite
